import 'package:flutter/material.dart';

class Gap12 extends StatelessWidget {
  const Gap12({super.key});

  @override
  Widget build(BuildContext context) => SizedBox(height: 12);
}

class Gap24 extends StatelessWidget {
  const Gap24({super.key});

  @override
  Widget build(BuildContext context) => SizedBox(height: 24);
}
